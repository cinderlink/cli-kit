/**
 * Tests for index.ts
 */

import { describe, test, expect } from 'bun:test'

describe('JSX Config Components Index', () => {
  test('should have tests', () => {
    expect(true).toBe(true)
  })
})
