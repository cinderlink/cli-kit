/**
 * Tests for index.ts
 */

import { describe, test, expect } from 'bun:test'

describe('Plugins API JSX Stores Index', () => {
  test('should have tests', () => {
    expect(true).toBe(true)
  })
})
