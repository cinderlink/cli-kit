/**
 * Plugin JSX Application Helpers - CLI Integration
 *
 * Utilities for creating and managing CLI-integrated plugins
 */

import type { JSXPlugin } from '@cli/jsx/types'

/**
 * Create a JSX plugin with CLI commands
 */
export function createCLIJSXPlugin(config: JSXPlugin): JSXPlugin {
  return config
}
