{
  "name": "test-jsx-app",
  "version": "1.0.0",
  "description": "A TUIX application created with jsx template",
  "type": "module",
  "scripts": {
    "dev": "bun app.tsx",
    "start": "bun app.tsx",
    "build": "bun build --compile --minify --bytecode --outfile dist/app src/index.ts",
    "test": "bun test"
  },
  "dependencies": {
    "tuix": "file:../",
    "effect": "^3.0.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.3.0"
  }
}