{
  "processes": [
    {
      "name": "worker-logger",
      "command": "bun",
      "args": ["worker-logger.ts"],
      "autostart": true,
      "restart": "on-failure",
      "restartDelay": 1000,
      "description": "Worker that logs random messages every few seconds"
    },
    {
      "name": "file-watcher",
      "command": "bun",
      "args": ["file-watcher.ts"],
      "autostart": true,
      "restart": "on-failure",
      "restartDelay": 2000,
      "description": "Watches for file changes and logs activity"
    },
    {
      "name": "failing-service",
      "command": "bun",
      "args": ["failing-service.ts"],
      "autostart": true,
      "restart": "never",
      "description": "Service that fails immediately to test error handling"
    }
  ]
}