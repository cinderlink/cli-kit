{
  "name": "@tuix/cli",
  "version": "1.0.0-rc.3",
  "description": "CLI building package providing command registration, routing, and execution",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "bun build src/index.ts --outdir dist --format esm --target bun",
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "@tuix/core": "workspace:*",
    "@tuix/components": "workspace:*",
    "@tuix/layout": "workspace:*",
    "@tuix/styling": "workspace:*",
    "@tuix/services": "workspace:*",
    "effect": "^3.0.0",
    "commander": "^11.1.0",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.3.0"
  }
}